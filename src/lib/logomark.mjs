export const LOGOMARK_VIEWBOX = Object.freeze({
  width: 440,
  height: 560,
});

export const LOGOMARK_BARS = Object.freeze([
  Object.freeze({ x: 0, y: 280, width: 80, height: 280, rx: 16 }),
  Object.freeze({ x: 120, y: 160, width: 80, height: 400, rx: 16 }),
  Object.freeze({ x: 240, y: 0, width: 80, height: 560, rx: 16 }),
  Object.freeze({ x: 360, y: 120, width: 80, height: 440, rx: 16 }),
]);

export const LOGOMARK_GRADIENT_STOPS = Object.freeze([
  Object.freeze({ offset: 0, color: "#FFC06A" }),
  Object.freeze({ offset: 0.45, color: "#FF9A4D" }),
  Object.freeze({ offset: 1, color: "#F26A3D" }),
]);

export const LOGOMARK_COLORS = Object.freeze({
  background: "#1c1612",
  tile: "#2a1f17",
  brass: "#d4a574",
});
