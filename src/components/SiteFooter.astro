---
import Logomark from "./Logomark.astro";
import NavIcon from "./NavIcon.astro";
import { footerGroups } from "../config/nav";
import { getLatestRelease } from "../lib/releases";

const latestRelease = await getLatestRelease();
const latestVersion = latestRelease?.tag ?? "latest release";
---

<footer class="footer">
  <div class="footer-inner">
    <div class="footer-brand">
      <Logomark color="#d4a574" />
      <span class="brand-word">Token Use</span>
    </div>
    <div class="footer-cols">
      {footerGroups.map((group) => (
        <div>
          <div class="footer-h">{group.heading}</div>
          {group.items.map((item) => (
            <a
              href={item.href}
              {...(item.external ? { target: "_blank", rel: "noopener noreferrer" } : {})}
            >
              {item.icon && <NavIcon icon={item.icon} size={13} />}
              {item.label}
            </a>
          ))}
        </div>
      ))}
    </div>
    <div class="footer-line">
      <span>2026 tokenuse - all your tokens, all on your machine.</span>
      <span>{latestVersion}</span>
    </div>
  </div>
</footer>
