---
import Logomark from "./Logomark.astro";
import NavIcon from "./NavIcon.astro";
import { headerNav } from "../config/nav";
---

<nav class="nav">
  <div class="nav-inner">
    <a class="brand" href="/">
      <Logomark gradient gradientId="site-nav-bars-gradient" />
      <span class="brand-word">Token Use</span>
    </a>

    <div class="nav-links" aria-label="Primary navigation">
      {headerNav.map((item) =>
        item.cta ? (
          <a
            href={item.href}
            class="nav-cta"
            {...(item.external ? { target: "_blank", rel: "noopener noreferrer" } : {})}
          >
            {item.icon && <NavIcon icon={item.icon} size={14} />}
            <span>{item.label}</span>
          </a>
        ) : (
          <a
            href={item.href}
            class="nav-text-link"
            {...(item.external ? { target: "_blank", rel: "noopener noreferrer" } : {})}
          >
            {item.icon && <NavIcon icon={item.icon} size={14} />}
            {item.label}
          </a>
        )
      )}
    </div>

    <details class="nav-menu">
      <summary aria-label="Open primary navigation">
        <svg viewBox="0 0 24 24" width="20" height="20" aria-hidden="true">
          <path d="M4 7h16M4 12h16M4 17h16" fill="none" stroke="currentColor" stroke-linecap="round" stroke-width="1.8"></path>
        </svg>
      </summary>
      <div class="nav-menu-panel" aria-label="Primary navigation">
        {headerNav.map((item) => (
          <a
            href={item.href}
            {...(item.external ? { target: "_blank", rel: "noopener noreferrer" } : {})}
          >
            {item.icon && <NavIcon icon={item.icon} size={16} />}
            <span>{item.label}</span>
          </a>
        ))}
      </div>
    </details>
  </div>
</nav>
