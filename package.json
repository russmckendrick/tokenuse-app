{
  "name": "tokenuse-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "favicons": "node scripts/generate-favicons.mjs",
    "sync:docs": "node scripts/sync-tokenuse-docs.mjs",
    "dev": "pnpm run sync:docs && pnpm run favicons && astro dev",
    "build": "pnpm run sync:docs && pnpm run favicons && astro build",
    "preview": "astro preview",
    "check": "pnpm run sync:docs && pnpm run favicons && astro check"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/cloudflare": "^13.5.0",
    "@plausible-analytics/tracker": "^0.4.5",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.1",
    "mermaid": "^11.14.0",
    "tailwindcss": "^4.3.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.90.0"
  }
}
